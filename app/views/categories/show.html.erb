<%= render "titleBar" %>
<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @category.description %>
</p>

<%= link_to 'Edit', edit_category_path(@category), class: 'button round' %>
<%= link_to 'Back', categories_path, class: 'button round' %>
