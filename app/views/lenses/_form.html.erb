<%= form_for(lens) do |f| %>
  <% if lens.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lens.errors.count, "error") %> prohibited this lens from being saved:</h2>

      <ul>
      <% lens.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit class: "button round" %>
  </div>
<% end %>
